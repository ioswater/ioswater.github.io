# 2026-07-30

## 博客文档维护规范 + 示例接入
- 用户询问博客文档/文件夹维护方法，并以桌面 `knowledge-tools-comparison.md` 为示例接入 liuluit 体系。
- Starlight 内容约定（已验证）：内容在 `src/content/docs/`；按主题建目录=栏目，autogenerate 目录必须有 `index.md` 落地页（否则该栏目 404）；侧边栏 `src/config/sidebar.json` 用 `autogenerate:{directory:"xxx"}` 自动列文章；frontmatter 需 `title`+`description`（惯用 `lastUpdated`）；正文不要写 H1（frontmatter title 即 H1，重复 H1 不优雅）；英文镜像 `en/<dir>/` 且需 `en/<dir>/index.md`。
- 示例接入：`ai-project-architecture/knowledge-tools-comparison.md`（补 frontmatter、去掉正文 H1），并在 `ai-project-architecture/index.md` 增加链接。构建通过（56 页）。Starlight i18n 行为：仅默认语言有文档时，另一语言路由会自动生成（带未翻译提示），不会构建报错；要纯净 EN 需补 `en/...` 文件。已提交 `codex f5fe8e4` 并 push origin。
- 核实「目录左右抽屉式开合」：磁盘源码其实已实现并早就提交（`codex 259d2cb` 抽屉式开合 + `ea7aaa6` 圆钮/箭头微调）。实现为 grid 轨道 `--toc-track` 从 16rem 动画收到 0px（正文占满让出空间）+ `.toc-panel` `translateX(100%)` 滑出；toggle 常驻右上角，收起态变圆钮+箭头反向+脉冲；移动端(<72rem)隐藏目录抽屉。上一轮只是卡在「部署」那步（后端报错），并非没改成功。
- 用户确认「可以，一起提交部署」：两项（新文档 f5fe8e4 + 抽屉改造 259d2cb/ea7aaa6）源码均已在 codex 提交，本次 `npm run build` 通过(56页) 并执行 `npm run deploy:github-pages` 成功 → `master 073cea6`（含 `ai-project-architecture/knowledge-tools-comparison/` 中英文路由与 TOC 抽屉样式）。线上经 Varnish 约 10 分钟滞后；本地 `npm run dev` 立即可验。

## 内容编辑器设计（DesignEngineTeam 请求）
- 用户要求「设计一个内容编辑器」管理博客必须内容（排序/简介等），明确**不编辑 md 正文**（正文本地编辑）。已先摸清真实约定：frontmatter 走 Starlight `docsSchema()`（必填 title+description，惯用 lastUpdated；项目当前无 order/slug/draft）；sidebar.json 顶层分组（autogenerate 目录需 index.md，否则 404）；排序当前纯按文件名（无 order 字段）→ 设计用 Starlight 原生 `order` 数字字段控制，不改 URL；EN 镜像 en/<dir>/<file>.md，缺则自动回退「未翻译」页。
- 产出：①设计规格 `design-proposal/content-editor-design.md`（范围/数据模型/三栏界面/功能清单 F1-F16/技术架构/与现有 5 步流程映射/开放问题）；②可交互原型 `design-proposal/content-editor-prototype.html`（侧边栏树拖拽排序、元数据表单、frontmatter+sidebar.json 实时预览、校验、EN 镜像面板，自包含示例数据）。
- 补充的必要功能（建议）：EN stub 生成、草稿/批量更新、搜索、落地页列表同步、新建脚手架、正文只读首段、主题跟随、未保存提示。待用户确认是否落地为真实本地 Web 编辑器（Vite+Node API，gray-matter 读写 frontmatter）。

## 内容编辑器 Web 版实现（已上线）
- 用户拍板 5 点需求 +「站点维护不对外、仅编辑器可见」→ 已**实现为真实 Web 应用**并部署。
- **架构决策**：编辑器以「文件系统扫描 + 两份配置」为准——`src/config/sidebar.json` = 公开栏目，`src/config/editor-internal.json` = 内部只读栏目（带「仅编辑器可见」徽标、不可编辑）。站点维护从 sidebar.json 的 main 移除，整体移入 editor-internal.json（其页面散落在 content/ 与 operations/ 目录，仍照常构建，只是不在公开导航）。
- **文件**：`editor/server.mjs`（Node 零依赖 http 服务，自写 frontmatter 解析器，读写保留正文；端点 GET/PUT/DELETE /api/article、DELETE /api/category、POST /api/apply 写 sidebar+internal+frontmatter(含新建栏目目录+index.md/新建文章/删除)、POST /api/deploy 执行 git commit + `npm run deploy:github-pages`）、`editor/public/{index.html,app.js,styles.css}`（三栏：栏目树拖拽排序/导入拖拽区/内部只读徽标 + 元数据表单 + 实时 frontmatter 预览与校验 + 应用变更弹框列变更并部署 + 主题切换）。`package.json` 加 `npm run editor` 脚本。`.gitignore` 加 `*.bak`（apply 自动备份）。
- **需求覆盖**：①删除文章/栏目 ②拖拽导入 .md（暂存→应用后写入目标目录）③栏目顺序拖拽 ④应用变更弹框列变更→确认后写文件+commit+deploy ⑤站点维护内部只读。
- **关键坑修复**：frontmatter 写入时空字符串字段必须 delete 而非写成 `key: ""`（否则 `order: ""` 会使 Starlight 构建报类型错误）；apply 前对 sidebar/internal 写 .bak。
- **已提交/部署**：`codex d2c5d39`（feat: 内容编辑器 + 站点维护移入内部只读栏目，8 files +1273/-36）；`master 49681e0` 部署生效（站点维护已退出公开导航）。线上 Varnish 约 10 分钟滞后；编辑器本地 `npm run editor` → http://localhost:5179。
- 设计文档 `design-proposal/content-editor-design.md` 已更新第 7/8/9 节反映最终决策与实现状态。
