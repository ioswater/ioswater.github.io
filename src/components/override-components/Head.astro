---
const { head } = Astro.locals.starlightRoute;
---

<meta name="theme-color" content="#040b17" />
<link rel="icon" href="/favicon.svg" type="image/svg+xml" sizes="any" />

<script is:inline>
  try {
    if (localStorage.getItem("reading-focus") === "on") {
      document.documentElement.classList.add("reading-focus");
    }
    // 默认折叠左侧导航（仅当用户显式展开过才展开），避免首屏正文被挤压
    if (localStorage.getItem("sidebar-collapsed") !== "false") {
      document.documentElement.classList.add("sidebar-collapsed");
    }
  } catch (e) {}
</script>

{head.map(({ tag: Tag, attrs, content }) => <Tag {...attrs} set:html={content} />)}
