name: Docs CI

on:
  push:
    branches: ["master", "main", "codex/dockit-source"]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Type and content checks
        run: npm run check

      - name: Build
        run: npm run build
